# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python-Core

on:
  push

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Validate ref_name
      run: |
        bash convention_checking.sh REF ${{ github.ref_type }} ${{ github.ref_name }}
    - name: Get changed files
      id: changed-files
      uses: jitterbit/get-changed-files@v1
      with:
        format: 'json'
    - name: Skip convention checking
      run: |
        readarray -t changed_files <<<"$(jq -r '.[]' <<<'${{ steps.changed-files.outputs.all }}')"
        echo "List of changed files: ${changed_files}"
        for file_name in ${changed_files[@]}; do
          if [ ${file_name} != *".md"* ] &&
            [ ${file_name} != *".ipynb"* ];
          then
            echo "Allow to check convention."
            exit 0
          fi
        done
        echo "Skip pipelines for convention checking."
        exit 1
    - name: Set up Python 3.11
      uses: actions/setup-python@v3
      with:
        python-version: "3.11"
    - name: Install dependencies
      run: |
        python3 --version
        python3 -m pip install --upgrade pip
        pip install -r ./requirements.txt
    - name: List files in the repository
      run: |
        cd ${{ github.workspace }}
        pwd
        ls -lah ./
    - name: Convention checking
      run: |
        # Check PEP8
        bash convention_checking.sh PEP8
        # Run unit tests
        bash convention_checking.sh TEST
